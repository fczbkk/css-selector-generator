/**
 * Creates chain if :nth-child selectors from root to the element.
 * @param {Element} element
 * @param {Element} root
 * @return {string}
 */
export function getFallbackSelector(element: Element, root: Element): string;
