import { CssSelector } from './types';
/**
 * Generates unique CSS selector for an element.
 */
export declare function getCssSelector(needle: unknown, custom_options?: {}): CssSelector;
export default getCssSelector;
